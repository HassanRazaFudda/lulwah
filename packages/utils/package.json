{
  "name": "@lulwah/utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Money, phone, slug and date formatting helpers shared across the API, storefront and admin (plan.md §6).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@lulwah/config": "workspace:*",
    "eslint": "^10.8.1",
    "typescript": "^6.0.0"
  }
}
